# Supabase — projeto talk2me (free tier, região sa-east-1)
# Copie este arquivo para .env.local e preencha. .env.local NÃO vai pro git.
# A publishable key é segura para uso client-side (SPA).
VITE_SUPABASE_URL=https://liaxcglgezrtbcrdegqa.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# Motor do avatar em LIBRAS (M5). 'on' usa o VLibras Player real no LibrasViewer
# da tela do Cliente; qualquer outro valor mantém o placeholder estilizado.
VITE_VLIBRAS=off
