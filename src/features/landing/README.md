# features/landing/

Landing Page — hero, como funciona, benefícios, demonstração, CTA, footer.

- **Marco:** M2 (telas estáticas) · formulário `demo_requests` no M4.
- **Dono sugerido:** Dev A (`landing/` + `home/`).
- **Referência:** [[Landing Page]] · [[Design System]].
- **Regra:** não importa `supabase` direto (exceto envio do formulário via canal/lib no M4).
